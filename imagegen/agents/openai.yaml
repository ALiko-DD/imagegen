interface:
  display_name: "ImageGen"
  short_description: "Native-format image Prompts with visual-noise control"
  default_prompt: "Use $imagegen to have the main agent create a validated native-format Prompt, generate the image, and verify the PNG."

policy:
  allow_implicit_invocation: true
